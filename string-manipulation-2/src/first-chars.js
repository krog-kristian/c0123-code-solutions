/* exported firstChars */
/* define a function named firstChars of parameters length and string
return a string spliced from 0 to length */

function firstChars(length, string) {
  return string.slice(0, (length));
}
