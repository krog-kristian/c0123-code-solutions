/* exported getLastChar */

/* define a function getLastChar with one parameter string
return the value of the length of the index minus one */

function getLastChar(string) {
  return string[string.length - 1];
}
