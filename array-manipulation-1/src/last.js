/* exported last */

/* declare a function name last with the parameter of array
return array at the index of the array length minus one */

function last(array) {
  return array[array.length - 1];
}
